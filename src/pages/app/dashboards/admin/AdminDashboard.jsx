// Archived duplicate: use the canonical dashboard at src/pages/dashboard/admin/AdminDashboard.jsx
// Backup copy preserved at AdminDashboard.backup.jsx
const AdminDashboard = () => null;

export default AdminDashboard;